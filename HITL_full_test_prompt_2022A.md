# 全量测试启动提示词（下一轮 · 2022A 波浪能最大输出功率设计 · 全新题 · 无优秀论文）

> 用途：在另一个 agent 环境跑一次**完整流水线**全量测试（继 2021C 之后的第二轮）。
> 题目：**2022A 波浪能最大输出功率设计**（题库 `benchmarks/problems/2022A/`，全新、未跑过）。
> 与 2021C 那轮的关键差异：①**机理流派（mech）首次真实出场**（2021C 派的是 opt/robust/surv）；②**实测新增的"去 AI 味"写后必跑门禁**（commit 5fbd080 的 `scripts/check_ai_style.py`，从未被真跑过）；③题型从"数据驱动评价+规划"换成"物理机理 ODE+约束优化"，流水线的另一条主干得到回归测试。
> 用法：整段粘贴为初始提示词发给 agent。

---

## 给 agent 的完整提示词

```
你现在要运行我建立的一套"数学建模竞赛全自动流水线"skill，做一次**完整流程测试**。请严格按以下要求执行。

【任务】用本 skill 从零跑一遍数学建模竞赛题目的完整建模流程：数据摸底 → HMML 方法检索 → Actor-Critic 精炼 → 求解 → 轻审稿 → 论文编写。本题是全新的，之前没解过。

【题目】2022A 波浪能最大输出功率设计
- 题面：C:\Users\姜世骥\.zcode\workspace\default\math-modeling-agent\benchmarks\problems\2022A\problem.md
- 数据附件（同目录 data\，读取 xlsx 用引号包住路径）：
  - "附件3.xlsx"（不同入射波浪频率下的附加质量、附加转动惯量、兴波阻尼系数、波浪激励力（矩）振幅——是**频率的函数**，按每问给定的圆频率取对应行/插值）
  - "附件4.xlsx"（浮子和振子的物理参数和几何参数值）
  - "result1-1.xlsx" / "result1-2.xlsx" / "result3.xlsx"（问题1/3 的结果存放模板，勿改名，必填）
  - "附件1.gif"、"附件2.gif"（垂荡、垂荡+纵摇动画——无法逐帧阅读，凭题面文字理解运动形式即可，如实说明，不编造动画细节）

【题目要点（先读题面，不要只看本摘要）】
装置由浮子（圆柱壳+圆锥壳）、振子、中轴、PTO（弹簧+阻尼器）构成；忽略中轴/底座/隔层/PTO 的质量与摩擦；海水无粘无旋，浮子受波浪激励力（矩）、附加惯性力（矩）、兴波阻尼力（矩）、静水恢复力（矩）——题面附录给出了四类力的机理定义，建模必须严格按此，不要混入其他物理效应。
- 问题1：只做垂荡。建立浮子+振子二体耦合运动模型，初始平衡于静水。ω=1.4005 s⁻¹。情形(1) 直线阻尼系数 10000 N·s/m；情形(2) 阻尼力与相对速度绝对值的幂成正比（比例 10000，幂 0.5，非线性）。算前 40 个波浪周期、0.2 s 网格的垂荡位移与速度 → result1-1/1-2.xlsx；论文报 10/20/40/60/100 s。
- 问题2：只做垂荡，PTO 平均输出功率最大化：(1) 常量阻尼系数∈[0,100000]（ω=2.2143）；(2) 幂律（比例∈[0,100000]，幂指数∈[0,1]）。求最大平均输出功率与最优参数。
- 问题3：垂荡+纵摇。中轴可绕转轴转动（扭转弹簧+旋转阻尼器），振子随中轴转动且沿中轴滑动——四自由度耦合模型。ω=1.7152，直线阻尼 10000 N·s/m、旋转阻尼 1000 N·m·s。算 40 周期 0.2 s 网格 → result3.xlsx；论文报五时刻。
- 问题4：垂荡+纵摇，直线+旋转阻尼系数联合优化（均∈[0,100000]），ω=1.9806，求最大平均输出功率。

【特别约定——本次测试与以往不同】
1. 全新题，**不要把历史题（烟幕弹、隔离桩、板凳龙、2021C 的评价-规划范式等）的模式硬套**。本题是"物理机理 ODE 建模+约束优化"，没有分类评价也没有离散规划，让模型选择跟着题面机理走。
2. **不提供优秀论文**。本题无 reference_answers.json，所有数值靠内部交叉验证保证（如双求解器互验、步长减半收敛性对比），"自洽≠正确"纪律照常。
3. **HITL 人工审校是测试核心**，必须真实运行，不能静默跳过。规则：到门禁先在对话流列待确认项+建议，停下等我回复（confirm/edit/regenerate/skip/abort）；缺席才降级 confirm 并落盘记录。
4. **新增实测项——"去 AI 味"写后必跑门禁**：论文正文完成后必须运行 `python scripts/check_ai_style.py`，把完整检测输出记入测试报告；命中项逐条评估是否真实问题，该改则改、不改则说明理由。这是本仓库 5fbd080 新增的门禁，从未被实测过。
5. **机理流派（mech）必须真实出场**：阶段1 检索命中后按 school 派 subagent，mech 的方向卡片要真实产出且质量达标（2021C 那轮派的是 opt/robust/surv，mech 一直没测）。
6. **题意易漏点（读题时重点确认，到阶段0门禁要跟我核对）**：
   - 附加质量/附加转动惯量/兴波阻尼系数/激励力（矩）振幅都是**频率的函数**，四问 ω 各不相同（1.4005/2.2143/1.7152/1.9806），必须逐问按 ω 从附件3 取值或插值，不能全题共用一个数；
   - 初始条件是"平衡于静水"（位移 0、速度 0），不是从波峰起算；
   - Q2/Q4 的目标是**平均**输出功率（阻尼力×相对速度的时间平均，按整周期取），不是瞬时功率峰值；
   - Q1 情形(2)是非线性阻尼（|相对速度|^0.5），线性 ODE 解法不适用，须数值积分，且 v=0 处幂函数不可导的数值处理要交代；
   - Q3 是四自由度耦合（浮子垂荡+纵摇、振子沿轴滑动+随轴转动），振子位移与中轴转角的几何耦合关系易错；激励力矩 L·cos(ωt) 只作用于纵摇；
   - "忽略中轴、底座、隔层及 PTO 的质量"——振子质量要计入，被忽略的只是中轴等部件；
   - 单位一致性（N·s/m、N·m·s、kg、m、弧度制）。
7. 数值规范：ODE 求解必须报告求解器、步长、收敛性验证（步长减半对比）；0.2 s 输出网格与积分步长是两回事，不得混淆。

【流程与 HITL 门禁交互】
按 skill 的九阶段推进（阶段 0→1→2→3→4→5a→5b-1→5b-2→5b-3）。到每个 🔴/🟡 门禁：先完成该阶段工作并整理"待确认项+建议"，然后停下在对话里问我，**必须等我回复后才能继续**；禁止自问自答、禁止替我确认、禁止为赶进度跳过门禁。我的回复记为：确认=放行；修改=按意见改后重跑；重做=换思路重跑；跳过=标记继续；中止=停。

门禁点：
- 阶段0 题意（🔴）：频率相关参数逐问取值、初始条件、平均功率定义、四自由度耦合、非线性阻尼、result 模板，我理解得对不对？
- 阶段1 方向（🟡）：检索命中方向是否覆盖机理+优化两路？mech 卡片质量如何？
- 阶段2 模型（🔴）：二体/四自由度耦合 ODE 的闭式写法、线性情形是否做"频域稳态解 vs 时域数值解"双路互验、幂律情形数值处理、平均功率目标函数——符合物理直觉吗？
- 阶段3 量级（🟡）：位移/速度/相位差/平均功率（W 量级）合理吗？收敛了吗？
- 阶段4 放行（🔴）：双路互验/步长收敛/交叉验证通过？result 模板填满？放行进写作吗？
- 阶段5a 报告（🔴）／5b-1 蓝图（🟡）／5b-2 正文（🔴，编译通过+去AI味检测已跑）／5b-3 结构审校（🔴）。

【启动步骤】
1. `.modeling/.health_checked` 已存在且 2026-08-29 重跑过 13/13，按 skill 规则跳过健康检测，直接进阶段 0。
2. 全程按 skill 纪律：反过拟合、竞赛合规（匿名/无目录/摘要独立/主节"一、二、三、"/AI 声明）、失败诚实（gif 读不了就说明、不收敛如实记录、不静默跳过）。

【交付】
- 各阶段 HITL 门禁交互记录；
- 关键数值（Q1 两情形五时刻位移速度、Q2/Q4 最大平均输出功率与最优阻尼系数、Q3 五时刻四自由度状态；全部可追溯）；
- 论文 PDF（编译通过）+ 建模报告 + result1-1/1-2/3.xlsx（回填）；
- 一份"流程测试报告"：门禁真实拦截记录；**去AI味检测的完整输出与处置**；mech 流派出场质量评价；数值收敛/交叉验证记录；题意易漏点核对表。

【环境提示】
Windows + git-bash。Python 用原生路径（C:/...）。xlsx 路径含中文/空格用引号。tectonic 在 PATH。绘图走 fig_helpers + SciencePlots + TikZ 规范，数值图用求解引擎真实数据。gif 动画无法逐帧读取——如实说明即可，别编造动画内容。
```

---

## 补充说明（给你看，不发给 agent）

**为什么选 2022A**：
- **mech 流派补位**：2021C 轮派出的方向卡片是 opt/robust/surv，机理与预测两个流派从未被真实测试。2022A 是纯机理 ODE+优化题，mech 必然出场且是主战场。
- **题型错开回归测试**：2021C 是"数据驱动评价+规划"，2022A 是"物理机理+连续优化+数值积分"，把流水线的另一条主干（ODE 求解、频域/时域互验、参数优化、数值收敛验证）压一遍。
- **附件齐全且含交付模板**：附件3/4 系数表 + result1-1/1-2/3 三个交付 xlsx（勿改名必填的门禁照常可测）；附件1/2 是 gif 动画，天然考"失败诚实"（读不了就说明，不编造）。
- **理论性质素材天然**：共振频率附近的单峰性、常阻尼情形的频域闭式稳态解 vs 时域数值解互验、"精确归约"纪律有真实落点。
- **去 AI 味门禁首测**：5fbd080 新增的 `check_ai_style.py` 写后必跑从未被实测，本轮纳入交付清单。

**排除的候选（附件体检结论，2026-08-29 实测）**：
- ~~2023C 蔬菜定价~~：**附件2（销售流水明细）缺失**——Q1-Q3 的命脉，不可跑；
- ~~2022B~~：data/ 里错放了 D 题 PDF，数据缺失；
- ~~2024E 交通管控~~：**附件2 缺失**；
- ~~2024A 板凳龙 / 2025A~~：有 reference_answers.json 但历史上已解过，"全新题"污染；
- 备选（若你不想跑物理题）：2023A 定日镜场（几何+大规模优化，附件+result 模板齐全）或 2023E 黄河水沙（数据分析+机理）。

**执行注意**：
- 本题数值陷阱多（非线性阻尼在 v=0 的奇性、频率插值、平均功率的时间平均窗口），正好考阶段 2/4 门禁的真实拦截力；
- 无 reference answers → 数值可信度全靠内部双路互验与收敛性验证，测试报告里要看这两项是否真做；
- 与 2021C 一样，HITL 需在能中途停下的交互会话里跑；若 agent 非交互跑批，降级路径会触发——如实记录即可。

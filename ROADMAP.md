# ROADMAP —— 评测驱动的可持续迭代方案

本项目的迭代引擎是 **benchmark**：任何 skill 改动（提示词、脚本、流程）都必须在基准题上重跑并证明总分不降，杜绝盲改。所有迭代活动围绕下述三层测试金字塔与版本节奏展开。

> 2026-08 精简重构：从「3 层解耦 + Artifact DAG + 具身自验证 + 6 流派并发 + 5 章节并行」精简为
> 「HMML 方法检索定向发散 → Actor-Critic 精炼 → 求解 → 轻审稿 → 串行写作」六阶段流水线。
> 本 ROADMAP 已同步该设计取向：**默认走最短路径，严谨只在真正需要的那一步才上**。

## 三层测试金字塔

| 层 | 内容 | 触发时机 | 成本 |
| :-- | :-- | :-- | :-- |
| L1 单元回归 | `tests/` pytest 套件（算法正确性、模板合规、SKILL 引用完整性）+ GitHub Actions CI | 每次 push | 秒级 |
| L2 端到端基准 | `benchmarks/` 真题全流程跑分（自动 + 人工） | 每轮功能迭代后 | 小时级 |
| L3 实战闭环 | 真实比赛/模拟赛产出与评委反馈回灌 | 赛后 | 天级 |

## 双产物交付制（核心流程约定）

1. **建模报告（给人看）**：阶段 5 第一步，`.modeling/manuscript/modeling_report.md`——一页速览 + 逐问题要点 + 符号附录，供论文手直接据此写作；**经人工审批门禁**。
2. **论文本身（给评委看）**：审批通过后，阶段 5 第二步**串行逐段**写作并编译为 `paper.pdf`（不再并行 5 章节）。
3. **benchmark 模式**跳过审批门禁直接出论文，用于自动化评测；建模报告仍照常生成，作为评分体系中的一个计权产物。
4. 长期目标：通过人机对比协议（见 benchmarks/scoring_rubric.md）持续缩小 AI 论文与优秀人类论文的差距。

## 版本节奏（semver + git tag）

- **每个版本绑定**：一次基准成绩（score.md）、成本记录（token 消耗 / 墙钟时间 / 人工干预次数）、CHANGELOG 条目。
- 迭代循环：选题 → 跑全流程 → 审计 issue 清单 → 按优先级修复 → L1 回归 → L2 重跑对比 → 发版打 tag。

## 冲刺期里程碑（现在 → 国赛）

| 里程碑 | 内容 | 状态 |
| :-- | :-- | :-- |
| M0 质量地基 | 脚本修复、模板合规、角色补齐、tests+CI | ✅ 已完成 |
| M1 精简重构落地 | 六阶段流水线 + HMML 检索 + 第7派 + 健康检测 + 真题库 | ✅ 已完成（2026-08） |
| M2 战斗可用 | 一道陌生题无人值守跑通全流程（检索→发散→精炼→求解→轻审稿→写作），产出可提交 PDF | 🚧 推进中 |
| M3 人机对比首轮 | 同题人写 vs AI 生成差异归因表完成，至少 3 条差异回灌进 roles/ | ⬜ |

## 技术债清单（按优先级）

- **P1 断点续跑**：各阶段产物落盘时写入 `phase_status.json`（阶段号、输入指纹、完成标志）；SKILL.md 提供 resume 规则——从最高完成阶段之后继续，禁止重算已完成阶段。
- **P1 scripts 扩容**：竞赛高频件——MILP 排产骨架、CVaR 求解器、熵权-TOPSIS、灰色预测 GM(1,1)，均按现有工具箱标准（random_state、warning 字段、docstring 用法示例）编写。
- **P2 检索精度**：`method_retrieve.py` 是关键词/概念匹配（轻量、省 token），可评估是否升级为 embedding 语义检索以获得更高召回（代价是增加依赖）。
- **P2 跨平台实测**：SKILL.md 平台适配表目前仅 Hermes 实测；在 Claude Code 与 Antigravity 各跑一道小规模基准题验证串行退化路径。

## 回灌机制（L3 → 提示词资产）

实战暴露的问题按类别回灌：
- 方法论缺陷 → `references/cumcm_reviewer_pitfalls.md` 新增条目
- 角色行为缺陷 → 对应 `roles/*.md` 补硬约束
- 数值算法缺陷 → scripts 工具箱修复 + tests 加回归用例
- 流程缺陷 → SKILL.md 状态机修订
每次回灌在 CHANGELOG 记录来源题目与证据。
